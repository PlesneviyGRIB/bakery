import {FC, PropsWithChildren} from "react";
import {Styled as S} from "./Model.styled"

interface ModelProps {

}

export const Model: FC<PropsWithChildren<ModelProps>> = ({}) => {
    return (
        <S.Model>

        </S.Model>
    )
}